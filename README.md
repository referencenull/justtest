# justtest

Simple motocross biker inventory web app.

## Run locally

From the repository root:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.

The app uses browser localStorage as a local test database and pre-populates 20 motocross products on first load.
